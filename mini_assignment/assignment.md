Goal: Develop a simple script in Python and containerize it. (be prepared to walk through your code in your IDE)

Create a python based script to do the following using pytest.
Use assert statements for compare numeric values.
Verify that a specified substring is present within a larger message.
Create a docker container.
Build the docker container locally.

Bonus: Create a GitHub workflow for your project.

The workflow should use "workflow_dispatch" for manual inputs.
Demonstrate passing and failing runs.