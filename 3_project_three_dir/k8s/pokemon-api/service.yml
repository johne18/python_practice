apiVersion: v1
kind: Service
metadata:
  name: pokemon-fastapi
spec:
  type: NodePort
  selector:
    app: pokemon-fastapi
  ports:
    - protocol: TCP
      port: 80 
      targetPort: 8000 # port where the app pod is listening
      nodePort: 30000