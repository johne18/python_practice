apiVersion: v1
kind: Service
metadata:
  name: pokemon-fastapi
spec:
  type: NodePort
  selector:
    app: pokemon-fastapi
  ports:
    - protocol: TCP
      port: 80 # port where pods can communicate with this container
      targetPort: 8000 # port where the app pod is listening
      nodePort: 30000